log.console = true
log.console.level = info

# Порог срабатывания механизма flow control (80% RAM по умолчанию)
vm_memory_high_watermark.relative = 0.8

# Минимум 50 МБ свободного места — ниже RabbitMQ блокирует публикацию
disk_free_limit.absolute = 50MB